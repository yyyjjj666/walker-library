#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_colors-cli@1.0.13@colors-cli/bin/colors" "$@"
  ret=$?
else 
  node  "$basedir/../_colors-cli@1.0.13@colors-cli/bin/colors" "$@"
  ret=$?
fi
exit $ret
